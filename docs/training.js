const form=document.getElementById("training-form"),status=document.getElementById("training-status");form?.addEventListener("submit",async e=>{e.preventDefault();status.className="form-status";if(!form.reportValidity())return;const fd=new FormData(form);if(fd.get("website"))return;const data=Object.fromEntries(fd.entries());delete data.website;delete data.consent;data._subject="Multi-Agent AI Training | "+data.company+" | "+data.format;data._template="table";data.submitted_at=new Date().toISOString();const endpoint=(window.AGENTIC_LEAD_ENDPOINT||"").trim();if(!endpoint){status.textContent="Private submission is not configured. Nothing was transmitted.";return}const button=form.querySelector(".lead-submit"),label=button.textContent;button.disabled=true;button.textContent="Sending securely...";try{const response=await fetch(endpoint,{method:"POST",headers:{Accept:"application/json","Content-Type":"application/json"},body:JSON.stringify(data)});if(!response.ok)throw new Error();status.textContent="Thank you. Your training inquiry was submitted. You can now choose a discovery-call time.";status.classList.add("success");const a=document.createElement("a");a.href="book.html";a.className="button secondary schedule-after-submit";a.textContent="Continue to discovery call";status.after(a);form.reset()}catch{status.textContent="The private submission service could not be reached. Nothing was posted publicly. Please try again.";status.classList.add("error")}finally{button.disabled=false;button.textContent=label}});
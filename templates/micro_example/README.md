# Example title

**Goal:** One sentence on what this example teaches.

**Primary users:** Who should open this folder.

**Human gate:** What must a person approve before any real side effect.

## Run

```bash
python3 run.py --offline
```

Optional live mode:

```bash
export ANTHROPIC_API_KEY=your_key
python3 run.py --live
```

## Layout

- `agents/` roles with one job each  
- `tools/` small helpers  
- `fixtures/` sample inputs  
- `run.py` entry point  

## Limits

List what this example does not do. Be honest.

## Book or course link

Chapter or lesson id goes here.
